# 📌 통합 버전 관리 (Git Workflow)

본 프로젝트는 `dev` 브랜치에서 개발을 진행하고, 안정화된 이후 `prod` 브랜치로 병합하여 배포하는 방식을 따릅니다.

## 🧑‍💻 일반적인 작업 흐름

1. **작업 내용 스테이징**
   수정사항을 모두 추적 대상으로 추가합니다.
   ```bash
   git add .
   ```

2. **커밋 작성**
   작업 내용을 요약하여 커밋 메시지를 남깁니다. (컨벤션에 맞게 작성 권장)
   ```bash
   git commit -m "feat: 새로운 대시보드 테마 추가"
   ```

3. **원격 dev 브랜치에 푸시**
   만약 원격 `dev` 변경사항이 있다면 `git pull origin dev`를 먼저 실행 후 병합합니다.
   ```bash
   git push origin dev
   ```

---

## 🚀 배포 워크플로우 (dev ➡️ prod)

로컬 `dev` 브랜치의 작업이 마무리되고 운영 환경으로 배포할 때 아래 순서를 반드시 따릅니다.

### 1단계: dev의 최신 상태 보장
배포 전, 본인의 로컬 `dev` 브랜치가 최신인지 확인합니다.
```bash
git pull origin dev
```

### 2단계: prod 브랜치로 이동
운영 브랜치로 체크아웃합니다.
```bash
git checkout prod
```

### 3단계: 반영 사항 병합 (Merge)
최신 `dev` 내용을 `prod`로 가져와 병합합니다.
```bash
git merge dev
```

### 4단계: 운영 서버 원격지 배포
병합된 최신 운영본을 원격지에 푸시하여 실 배포를 진행합니다.
```bash
git push origin prod
```

### 5단계: 개발 브랜치로 복귀
수고하셨습니다. 다시 신규 개발을 위해 `dev` 브랜치로 전환합니다.
```bash
git checkout dev
```

---

## ⚠️ 주의 사항
- `prod` 브랜치에 직접 커밋하거나 강제 푸시(`-f`)하는 동작은 **절대 금지**합니다. 
- 복잡한 충돌(Conflict)이 발생할 경우 `dev`에서 먼저 병합하고 테스트를 통과한 뒤 위 워크플로우를 진행하세요.
