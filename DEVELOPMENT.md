# 개발 가이드라인 (Development Guidelines)

본 프로젝트의 개발 및 운영 시 준수해야 할 핵심 규칙입니다.

### 🐚 쉘 환경 (Shell Environment)
- **모든 쉘 커맨드는 PowerShell이 아닌 일반 CMD 문법으로 작성하고 실행해야 합니다.**
- **CMD 실행 시 한글 깨짐 방지를 위해 반드시 `chcp 65001` 명령어를 선행하여 UTF-8 환경을 확보해야 합니다.**
  - 예: `cmd /c "chcp 65001 && gradlew.bat ..."`
- 에이전트(AI)가 커맨드를 제안할 때도 반드시 CMD 호환성과 UTF-8 환경을 우선시합니다.
  - 예: `copy /y` (O), `Copy-Item` (X)
  - 예: `dir` (O), `Get-ChildItem` (X)

### 🎨 프론트엔드 및 렌더링 (Frontend & Rendering)
- **60FPS 유지**: X-View와 같은 대량의 데이터를 다루는 캔버스 렌더링 시, 60fps를 유지하기 위해 루프 내 무거운 연산(나눗셈, 함수 호출 등)을 최소화하고 상수로 호이스팅(Hoisting)하여 처리합니다.
- **Batch Drawing**: 수만 개의 점을 그릴 때 `beginPath()`와 `fill()` 호출을 그룹화하여 WebGL/Canvas 상태 변화에 따른 오버헤드를 줄입니다.
- **공통 로직 단일화**: Y축 상한선(`yMax`) 계산 등 차트의 여러 요소(그리드, 점, 하이라이트)에 영향을 주는 로직은 반드시 하나의 헬퍼 함수로 관리하여 좌표 어긋남(Drift)을 방지합니다.

### 🛡️ 데이터 정합성 (Data Integrity)
- **Unique 식별**: 차트의 개별 점(Transaction)은 반드시 고유한 ID를 가져야 하며, 중복이 발생할 우려가 있는 경우 시각(Timestamp)을 조합한 복합 식별자를 사용하여 선택(Selection)의 무결성을 확보합니다.
- **방어적 에러 판별**: 트랜잭션의 에러 여부를 판단할 때 `isError` 필드뿐만 아니라 `httpStatusCode >= 400`, `error` 객체 존재 여부 등을 다중 검사하여 사용자에게 정확한 상태 정보를 제공합니다.
- **Deduplication Window**: 장애 상황에서 동일한 성격의 로그가 폭발적으로 유입되는 것을 방지하기 위해, 수집 서버 단에서 **인시던트별 중복 제거 윈도우(AppError 30s, SQL 10s)**를 반드시 적용하여 DB 부하를 제어합니다.

### ♻️ 재사용성 및 모듈화 (Reuse & Modularity)
- **중복 배제(DRY)**: 2회 이상 반복되는 로직은 반드시 공통 유틸리티나 상위 인터페이스로 추출하여 캡슐화하십시오.
- **관심사 분리**: 각 클래스는 고유의 책임(Responsibility)에 집중하게 하며, 서비스 간의 결합도를 낮추어 재사용성을 높입니다.
- **예외 없는 중복 관리**: 아주 단순한 로직이라도 프로젝트 전반에서 공유된다면 공통 모듈화를 우선적으로 고려합니다.

### 💎 코드 품질 및 가독성 (Code Quality & Readability)
- **하드코딩 지양 (Anti-Hardcoding)**: 문자열, 숫자, 설정값 등을 코드 내에 직접 입력하지 않습니다. 의미 있는 이름을 가진 상수(`static final`, `const`)나 외부 설정 파일(`properties`, `yml`, `env`)로 관리하여 변경에 유연하게 대처하십시오.

### 🧩 아키텍처 및 리팩토링 (Architecture & Refactoring)
- **코드 무게 감량 (Anti-Gravity)**: 불필요한 복합 키나 복잡한 로직은 현재 데이터 전제로 해결 가능하다면 즉시 단순화합니다. (무거운 코드는 기술 부채이며 시스템의 속도를 늦춥니다.)
- **Lazy Loading**: 상세 로그와 같은 무거운 데이터는 차트 렌더링 시점에 들고 있지 않고, 사용자가 클릭하는 순간에만 백엔드에서 비동기로 가져오는 패턴을 유지합니다.

### 📝 문서화 (Documentation)
- **프롬프트 및 아티팩트**: `Implementation Plan` 및 `Walkthrough` 아티팩트 파일은 반드시 **한글**로 작성해야 합니다. 이는 협업 과정에서의 의사소통 효율을 최우선으로 하기 위함입니다.
